# uMap configuration (Django settings)
# Auto-generated by FSN Platform

# uMap uses Django social-auth for OIDC
# Install: pip install social-auth-app-django
# This is a Python settings file

AUTHENTICATION_BACKENDS = (
{% if services.kanidm is defined %}
    'social_core.backends.open_id_connect.OpenIdConnectAuth',
{% endif %}
    'django.contrib.auth.backends.ModelBackend',
)

{% if services.kanidm is defined %}
# Kanidm OIDC configuration
SOCIAL_AUTH_OIDC_OIDC_ENDPOINT = 'https://{{ services.kanidm.domain }}/oauth2/openid/{{ vault_umap_oidc_client_id | default("umap") }}'
SOCIAL_AUTH_OIDC_KEY = '{{ vault_umap_oidc_client_id | default("umap") }}'
SOCIAL_AUTH_OIDC_SECRET = '{{ vault_umap_oidc_client_secret }}'
SOCIAL_AUTH_OIDC_SCOPE = ['openid', 'email', 'profile']

# Auto-create users from OIDC
SOCIAL_AUTH_OIDC_CREATE_USERS = True

# Login URL
LOGIN_URL = '/login/oidc/'
{% endif %}

# Tile layers
UMAP_CUSTOM_STAMEN_TONER = {
    "name": "Stamen Toner",
    "url": "https://tiles.stadiamaps.com/tiles/stamen_toner/{z}/{x}/{y}.png",
    "attribution": "Map tiles by Stamen Design, under CC BY 3.0",
}
