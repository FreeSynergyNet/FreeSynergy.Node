// CryptPad SSO plugin configuration
// Auto-generated by FSN Platform
// Plugin: https://github.com/cryptpad/sso

module.exports = {
    // Enable SSO login
    enabled: {{ cryptpad_sso_enabled | default('true') | lower }},

    // Block non-SSO registrations (enforce SSO-only)
    enforced: {{ cryptpad_sso_enforced | default('false') | lower }},

    // Allow users to add a CryptPad password on top of SSO
    cpPassword: {{ cryptpad_sso_cp_password | default('true') | lower }},

    // Force users to set a CryptPad password
    forceCpPassword: {{ cryptpad_sso_force_password | default('false') | lower }},

    // SSO providers
    list: [
        {
            name: '{{ cryptpad_sso_provider_name | default("kanidm") }}',
            type: 'oidc',
            url: 'https://{{ services.kanidm.domain }}/oauth2/openid/{{ vault_cryptpad_oidc_client_id | default("cryptpad") }}',
            client_id: '{{ vault_cryptpad_oidc_client_id | default("cryptpad") }}',
            client_secret: '{{ vault_cryptpad_oidc_client_secret }}',
            jwt_alg: 'ES256',
            username_scope: 'profile',
            username_claim: 'preferred_username',
            use_pkce: true,
            use_nonce: true
        }
    ]
};
