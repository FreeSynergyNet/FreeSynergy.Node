// CryptPad config.js
// Auto-generated by FSN Platform – do not edit manually
// Official docs: https://docs.cryptpad.org/en/admin_guide/customization.html

module.exports = {
    httpUnsafeOrigin: 'https://{{ service_domain }}',
    httpSafeOrigin: 'https://{{ vars.sandbox_domain }}',

    httpAddress: '0.0.0.0',
    httpPort: {{ module.port }},

    // Admin keys (add your public signing key after first login)
    adminKeys: [
{% for key in cryptpad_admin_keys | default([]) %}
        '{{ key }}',
{% endfor %}
    ],

    // Storage paths (must match volume mounts)
    filePath: './datastore/',
    archivePath: './data/archive',
    pinPath: './data/pins',
    taskPath: './data/tasks',
    blockPath: './block',
    blobPath: './blob',
    blobStagingPath: './data/blobstage',
    decreePath: './data/decrees',

    // Log level
    logLevel: '{{ log_level | default("info") }}',
    logToStdout: true,

    // Inactive account cleanup (days)
    inactiveTime: {{ cryptpad_inactive_time | default(90) }},
    archiveRetentionTime: {{ cryptpad_archive_retention | default(15) }},

    // Max upload size (bytes, default 20MB)
    maxUploadSize: {{ cryptpad_max_upload | default(20971520) }},

    // Disable telemetry
    installMethod: 'unspecified',
};
